1. **修改Mock配置tab**：
   - 在el-form表单中添加保存按钮，点击后调用saveMockConfig方法
   - 移除el-input-number和el-switch的@change事件，让用户手动点击保存按钮

2. **修改发送请求tab**：
   - 将el-input-number组件替换为只读文本展示
   - 确保显示的数据是从mockConfig中获取的已保存数据

3. **调整saveMockConfig方法**：
   - 确保保存成功后，更新requestForm.mock_count的值

4. **重新构建前端代码**：
   - 运行npm run build命令，将修改后的前端代码构建到后端目录

5. **重启后端服务**：
   - 确保服务能够加载最新的前端资源