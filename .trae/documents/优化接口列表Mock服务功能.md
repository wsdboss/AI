我需要修改InterfaceListView.vue文件，实现以下功能：

1. **未生成状态根据Mock服务启用状态变换**：
   - 修改接口状态标签的显示逻辑，根据Mock服务启用状态改变样式
   - 当Mock服务启用时，未生成状态的样式可能需要调整

2. **优化Mock服务开关功能**：
   - 移除"部分启用"按钮
   - 为全量Mock开关添加change事件处理
   - 当开关打开时，自动将所有接口的Mock服务设置为启用状态
   - 当开关关闭时，保持各接口当前的Mock服务状态

3. **修改界面文本**：
   - 更新开关的文本显示，改为"是否全部启用"
   - 调整相关提示文本，使其更符合新功能

4. **添加事件处理函数**：
   - 添加handleAllMockEnabledChange函数，处理全量Mock开关的变化
   - 在函数中遍历所有接口，更新它们的Mock服务状态

5. **优化样式**：
   - 确保修改后的界面样式保持一致和美观
   - 调整未生成状态标签的样式，使其根据Mock服务状态变化

修改的主要文件是：`frontend/views/InterfaceListView.vue`

实施步骤：
1. 修改Mock服务开关的模板和事件
2. 添加全量Mock开关的事件处理函数
3. 修改接口状态标签的显示逻辑
4. 更新相关文本和样式
5. 测试功能是否正常工作